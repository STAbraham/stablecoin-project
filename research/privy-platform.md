# Privy platform — shared digest

**Scope:** shared — consumed by the incumbent architecture (tracker C-PR-1..7) and all tracks (Privy is the wallet layer in every posture). Privy **Earn** material is out of scope here → `../tracks/c-usdc-vasp-privy/research/privy-earn.md` (Track C only).
**Status:** `PENDING Steve's Privy docs`. Every claim cites a doc (name + page/section) before losing its PENDING tag. Facts landing here that answer a tracker item update `../counterparty-counsel-tracker.md` directly (status + source), per its source discipline.

## Wallet ownership & configuration (→ C-PR-1, C-PR-2)
_PENDING._ User-sole-owner configuration; whether any server/Zed authorization key or signer is required for the intended UX; policy/quorum model.

## Export & recovery (→ C-PR-3, C-PR-7)
_PENDING._ Recovery paths independent of Zed; export mechanics; practical post-export usability for the chosen chain / smart-wallet model.

## Transaction signing & open-loop sends (→ C-PR-4, C-PR-5)
_PENDING._ User-signed sends to arbitrary addresses; signing directly to third-party (e.g., redemption) addresses; UX constraints.

## Policy controls without custody (→ C-PR-6)
_PENDING._ Which controls (allowlists, limits, screening hooks) exist that do NOT give Zed unilateral asset control.

## Chains, assets, fees, limits
_PENDING._ Supported chains (Base? others), gas handling/sponsorship, pricing model, rate limits.

## Compliance & geography
_PENDING._ KYC division of labor, PH availability/restrictions, Privy's own licensing posture and terms Zed must pass through.

## Facts that surprised us / contradict assumptions
_(also logged in `../tracks/TRACKS.md` Decision log if they move the track comparison, and in the tracker if they flip a C-PR item)_

## Source docs
| Doc | Received | Processed | Digests it fed |
|---|---|---|---|
| — | — | — | — |
